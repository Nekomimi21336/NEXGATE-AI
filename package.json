{
  "name": "nexgateai-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NexgateAI Frontend - Vite + TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "dompurify": "^3.2.0"
  }
}
